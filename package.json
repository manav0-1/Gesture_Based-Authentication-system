{
  "name": "gesture-auth-system",
  "version": "2.0.0",
  "description": "Gesture Authentication System — MERN + MediaPipe + DTW",
  "scripts": {
    "install-all": "cd frontend && npm install --legacy-peer-deps && cd ../backend && npm install",
    "start": "concurrently \"cd backend && npm start\" \"cd frontend && npm run dev\"",
    "clean": "rm -rf frontend/node_modules backend/node_modules frontend/package-lock.json backend/package-lock.json"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
